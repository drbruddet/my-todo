template(name="list")
	a.item(class='{{#if private}}private{{/if}}')
		i.delete.fa.fa-times.right
		if isOwner
			span.toggle-private
				if private
					i.fa.fa-lock.private-lock
				else
					i.fa.fa-unlock.public-lock
		span.text #{text}
		.ui.teal.left.pointing.label
			span 1
